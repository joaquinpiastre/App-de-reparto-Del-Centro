// Logística reutiliza la misma vista de historial que el admin: solo lectura
// (recorridos, lugares visitados y pedidos por jornada), sin acciones de edición.
export { default } from '../(admin)/historial';
