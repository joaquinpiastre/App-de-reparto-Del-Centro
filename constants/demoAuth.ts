/**
 * Credenciales de demostración (sin Firebase todavía).
 * La contraseña es siempre 4 dígitos; el rol depende del usuario.
 */
export const DEMO_PIN = '1234';

/** Acceso al panel admin (dueño). */
export const DEMO_ADMIN_USER = 'admin';

/** Acceso app repartidor. */
export const DEMO_REPARTIDOR_USER = 'carlos';
